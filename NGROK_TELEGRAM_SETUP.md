# 🌐 Настройка Telegram бота для работы с ngrok

## 📋 Почему нужен ngrok?

Telegram требует **публичный HTTPS домен** для OAuth авторизации. Localhost не работает!

## 🚀 Быстрая настройка

### Шаг 1: Запуск ngrok

```bash
# Запустите скрипт
START_WITH_NGROK.bat
```

Или вручную:
```bash
# Установка ngrok
npm install -g ngrok

# Запуск для бэкенда
ngrok http 3001

# Запуск для фронтенда (в другом терминале)
ngrok http 5173
```

### Шаг 2: Получение публичных URL

После запуска ngrok покажет URL вида:
```
Forwarding  https://abc123-def456.ngrok.io -> http://localhost:3001
```

**Скопируйте HTTPS URL!**

### Шаг 3: Настройка бота Telegram

1. **Найдите @BotFather** в Telegram
2. **Отправьте команду:** `/mybots`
3. **Выберите ваш бот:** `My Space Diary`
4. **Нажмите "Bot Settings"**
5. **Выберите "Domain"**
6. **Введите ваш ngrok URL:** `https://abc123-def456.ngrok.io`
7. **Подтвердите**

### Шаг 4: Обновление конфигурации

Откройте файл `telegram-services/telegram-bot/.env` и обновите:
```env
API_URL=https://abc123-def456.ngrok.io
```

### Шаг 5: Перезапуск

1. **Остановите все процессы**
2. **Запустите проект заново:**
   ```bash
   START_SEPARATE.bat
   ```

## 🔄 Полная последовательность

1. **Запустите ngrok:**
   ```bash
   START_WITH_NGROK.bat
   ```

2. **Скопируйте URL** из окна ngrok (HTTPS)

3. **Настройте домен в @BotFather:**
   - `/mybots` → `My Space Diary` → `Bot Settings` → `Domain`
   - Введите: `https://xxxx-xxxx-xxxx.ngrok.io`

4. **Обновите .env файл:**
   ```env
   API_URL=https://xxxx-xxxx-xxxx.ngrok.io
   ```

5. **Перезапустите проект:**
   ```bash
   START_SEPARATE.bat
   ```

## 🎯 Готово!

Теперь авторизация будет работать:

1. **Откройте сайт:** `https://xxxx-xxxx-xxxx.ngrok.io`
2. **Нажмите "Войти через Telegram"**
3. **В Telegram:** отправьте `/web` боту
4. **Перейдите по ссылке** (теперь будет работать!)
5. **Нажмите "Start"**

## ⚠️ Важные моменты

- **Только HTTPS URL** - HTTP не работает
- **Домен должен быть точным** - без слэша в конце
- **Ngrok бесплатный** - но URL меняются при перезапуске
- **Для продакшена** нужен настоящий домен

## 🔧 Если не работает

1. **Проверьте домен в @BotFather**
2. **Убедитесь что используете HTTPS**
3. **Перезапустите бота после изменения .env**
4. **Проверьте что ngrok работает**

---

**🌌 Теперь Telegram авторизация будет работать!**
