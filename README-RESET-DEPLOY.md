# Скрипты для полной очистки и развертывания проекта

## 📋 Описание

Скрипты предназначены для полной очистки проекта, сброса всех локальных изменений к последнему коммиту на GitHub и развертывания приложения с нуля.

## 🚀 Использование

### Для Linux/Mac:
```bash
chmod +x reset-and-deploy.sh
./reset-and-deploy.sh "Ваше сообщение коммита"
```

### Для Windows:
```cmd
reset-and-deploy.bat "Ваше сообщение коммита"
```

## ⚡ Что делают скрипты

### 1. 🔄 Очистка Git
- `git reset --hard HEAD` - сбрасывает все измененные файлы
- `git clean -fdx` - удаляет все неотслеживаемые файлы и директории
- `git fetch origin && git reset --hard origin/main` - получает последние изменения с GitHub

### 2. 🧹 Очистка проекта
- Остановка Docker контейнеров (`docker-compose down`)
- Удаление `node_modules`, `dist`, `build`, `.next`
- Удаление логов и временных файлов
- Очистка Docker системы (`docker system prune -f`)

### 3. 📦 Установка зависимостей
- `npm ci` в корне проекта
- `npm ci` в `backend/`
- `npm ci` в `frontend/`

### 4. 🐳 Сборка и запуск Docker
- `docker-compose build --no-cache` - полная пересборка образов
- `docker-compose up -d` - запуск в фоновом режиме
- Ожидание 15 секунд для запуска сервисов
- Проверка статуса контейнеров

### 5. 📤 Коммит и пуш
- Если есть изменения - добавляет все файлы
- Создает коммит с указанным сообщением
- Отправляет изменения на GitHub

### 6. 🔍 Проверка работоспособности
- Проверка health endpoint приложения
- Вывод статуса контейнеров
- Показ доступных URL

## 📊 Вывод скрипта

Скрипт выводит подробный лог каждого шага:
- ✅ Успешное выполнение
- ⚠️ Предупреждения
- ❌ Ошибки
- ℹ️ Информационные сообщения

## 🔧 Полезные команды после выполнения

```bash
# Просмотр логов всех сервисов
docker-compose logs -f

# Просмотр логов конкретного сервиса
docker-compose logs -f backend
docker-compose logs -f frontend
docker-compose logs -f nginx

# Проверка статуса контейнеров
docker-compose ps

# Перезапуск всех сервисов
docker-compose restart

# Перезапуск конкретного сервиса
docker-compose restart backend

# Остановка всех сервисов
docker-compose down

# Полная пересборка
docker-compose down && docker-compose build --no-cache && docker-compose up -d
```

## 📱 Доступные URL после развертывания

- **Фронтенд**: `http://[IP-сервера]:3000`
- **API**: `http://[IP-сервера]:3000/api`
- **Health check**: `http://[IP-сервера]:3000/health`

## ⚠️ Важные моменты

1. **Резервное копирование**: Скрипт полностью удаляет все локальные изменения. Убедитесь, что важные изменения сохранены в GitHub или сделаны бэкапы.

2. **Сетевые настройки**: Скрипт использует IP-адрес сервера для проверки работоспособности. Убедитесь, что порт 3000 доступен.

3. **Права доступа**: Для Linux/Mac может потребоваться `sudo` для Docker операций.

4. **Зависимости**: Убедитесь, что установлены:
   - Git
   - Node.js и npm
   - Docker и docker-compose
   - curl (для проверки работоспособности)

## 🐛 Устранение проблем

### Проблема: "Permission denied"
```bash
# Linux/Mac
sudo chmod +x reset-and-deploy.sh
sudo ./reset-and-deploy.sh "Сообщение"
```

### Проблема: "Docker daemon not running"
```bash
# Запуск Docker
sudo systemctl start docker
# или
sudo service docker start
```

### Проблема: "npm ci failed"
```bash
# Очистка npm кэша
npm cache clean --force
# Повторный запуск скрипта
```

### Проблема: "Git push failed"
```bash
# Проверка прав доступа к репозиторию
git remote -v
# Настройка credentials
git config --global user.name "Your Name"
git config --global user.email "your.email@example.com"
```

## 🔄 Повторное использование

Скрипты можно использовать многократно для:
- Быстрого сброса к чистому состоянию
- Тестирования изменений из чистого окружения
- Развертывания на новых серверах
- Восстановления после проблем с разработкой
