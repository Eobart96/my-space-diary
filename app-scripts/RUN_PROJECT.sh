#!/bin/bash

# 🚀 My Space - Запуск проекта
echo
echo "╔══════════════════════════════════════════════════════════════╗"
echo "║                    🚀 MY SPACE - ДНЕВНИК                      ║"
echo "║                                                              ║"
echo "║  Запуск Frontend, Backend и Telegram бота                      ║"
echo "╚══════════════════════════════════════════════════════════════╝"
echo

# Проверяем наличие Node.js
if ! command -v node &> /dev/null; then
    echo "❌ Node.js не найден! Пожалуйста, установите Node.js с https://nodejs.org"
    exit 1
fi

# Проверяем наличие .env файла
if [ ! -f "Backend/.env" ]; then
    echo "⚠️ Файл Backend/.env не найден!"
    echo
    echo "Пожалуйста, создайте его на основе Backend/.env.example:"
    echo "1. Скопируйте Backend/.env.example в Backend/.env"
    echo "2. Добавьте ваш TELEGRAM_BOT_TOKEN от @BotFather"
    echo
    exit 1
fi

# Проверка зависимостей Frontend
echo "📦 Проверка зависимостей Frontend..."
if [ ! -d "../node_modules" ]; then
    echo "📦 Установка зависимостей Frontend..."
    cd ..
    npm install
    if [ $? -ne 0 ]; then
        echo "❌ Ошибка установки зависимостей Frontend"
        cd app-scripts
        exit 1
    fi
    cd app-scripts
fi

# Проверка зависимостей Backend
echo "📦 Проверка зависимостей Backend..."
if [ ! -d "../Backend/node_modules" ]; then
    echo "📦 Установка зависимостей Backend..."
    cd ../Backend
    npm install
    if [ $? -ne 0 ]; then
        echo "❌ Ошибка установки зависимостей Backend"
        cd ../app-scripts
        exit 1
    fi
    cd ../app-scripts
fi

echo
echo "🚀 Запуск проекта..."
echo "📍 Frontend: http://localhost:5173"
echo "📍 Backend:  http://localhost:3001"
echo "📍 Telegram: Бот готов к работе"
echo
echo "💡 Нажмите Ctrl+C для остановки всех серверов"
echo

# Запускаем проект
node ../app-scripts/start.js
