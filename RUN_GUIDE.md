# 🚀 My Space - Руководство по запуску

## 📋 Способы запуска

### 🔄 **Рекомендуемый: Раздельный запуск**
Компоненты работают в отдельных терминалах и не мешают друг другу.

```bash
# Запустить раздельное меню
START_SEPARATE.bat
```

Или выберите опцию 4 в главном меню `START.bat`.

### 🌐 **Только веб-приложение**
```bash
# Запустить веб-приложение
START_WEB.bat
```

### 🤖 **Только Telegram бот**
```bash
# Запустить Telegram бота
START_BOT.bat
```

### 🚀 **Все вместе (в одном терминале)**
```bash
# Запустить всё вместе
START.bat
# Выбрать опцию 3
```

---

## 📁 Структура проекта

```
My Space/
├── 🌐 Веб-приложение
│   ├── src/                 # React компоненты
│   ├── Backend/             # Node.js API
│   └── app-scripts/         # Скрипты запуска
├── 🤖 Telegram бот
│   └── telegram-services/   # Python бот и БД
├── 🚀 Скрипты запуска
│   ├── START.bat           # Главное меню
│   ├── START_WEB.bat       # Только веб
│   ├── START_BOT.bat       # Только бот
│   └── START_SEPARATE.bat  # Раздельный запуск
```

---

## ⚙️ Настройка

### 🔑 Telegram бот
1. Создайте бота в Telegram через @BotFather
2. Скопируйте токен в `telegram-services/telegram-bot/.env`
3. Запустите бота через `START_BOT.bat`

### 🌐 Веб-приложение
1. Установите Node.js (если не установлен)
2. Запустите через `START_WEB.bat`
3. Откройте http://localhost:5173

---

## 🎯 Команды Telegram бота

### 📝 **Основные команды**
- `/start` - начать работу
- `/help` - справка
- `/entries` - показать записи
- `/add` - добавить запись
- `/search <текст>` - поиск
- `/stats` - статистика

### 🔄 **Синхронизация**
- `/sync_status` - статус синхронизации
- `/sync_enable` - включить синхронизацию
- `/sync_disable` - отключить синхронизацию
- `/sync_now` - синхронизировать сейчас

---

## 🔧 Устранение проблем

### ❌ **Бот не запускается**
- Проверьте токен в `.env` файле
- Убедитесь, что Python установлен
- Проверьте интернет-соединение

### ❌ **Веб-приложение не запускается**
- Проверьте установку Node.js
- Очистите кэш: `npm cache clean --force`
- Переустановите зависимости: `npm install`

### ❌ **Порты заняты**
- Закройте другие процессы на портах 3001 и 5173
- Измените порты в конфигурационных файлах

---

## 🌐 Доступ к приложению

- **Веб-интерфейс**: http://localhost:5173
- **API**: http://localhost:3001
- **Telegram бот**: через @BotFather токен

---

## 💡 Советы

1. **Используйте раздельный запуск** для стабильной работы
2. **Первый запуск** может занять время (установка зависимостей)
3. **Сохраняйте токен** в безопасном месте
4. **Резервное копирование** БД находится в `telegram-services/telegram-bot/users.db`

---

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи в терминалах
2. Убедитесь, что все зависимости установлены
3. Перезапустите компоненты по отдельности
